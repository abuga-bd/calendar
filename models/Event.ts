export default interface Event {
    id:number,
    title: string,
    description: string,
    startDate: string,
    endDate: string,
    hasNext: boolean,
    priority: number,
    colorIndex: number
}